SELECT count(*) AS n, sum(CASE WHEN p.p_type LIKE 'PROMO%' THEN li.l_extendedprice * (1 - li.l_discount) ELSE 0 END) AS promo_rev, sum(li.l_extendedprice * (1 - li.l_discount)) AS total_rev FROM (SELECT explode(lineitems) AS li FROM orders_nested) t JOIN part p ON li.l_partkey = p.p_partkey
