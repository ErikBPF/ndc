# tpch-ndc bench profile config (per workspace). Sourced by run.sh.
# D11: execution target. Implemented: local. Declared, not implemented: k3s.
CLUSTER=local
# D12: storage target. Implemented: local-fs. Declared, not implemented: s3-cygnus
# (fleet MinIO on Cygnus :9191; requires hadoop-aws jars + Vault-provisioned creds).
FS=local-fs
# S3 placeholders for the s3-cygnus target (credentials via OpenBao handoff, never inline):
# S3_ENDPOINT=http://cygnus:9191
# S3_BUCKET=tpch-ndc
